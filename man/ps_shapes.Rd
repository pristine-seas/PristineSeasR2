% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapes.R
\name{ps_shapes}
\alias{ps_shapes}
\title{Get Pristine Seas shapes}
\usage{
ps_shapes(palette = NULL)
}
\arguments{
\item{palette}{Character. Name of the shape palette to retrieve. If
\code{NULL}, returns the available palette names.}
}
\value{
If \code{palette} is \code{NULL}, a character vector of palette names.
Otherwise, a named integer vector of R point shapes (see
\code{\link[graphics:points]{graphics::points()}}).
}
\description{
Retrieve Pristine Seas point-shape palettes by name. The companion to
\code{\link[=ps_colors]{ps_colors()}} for classes that are carried by form rather than hue.
}
\details{
The \code{"habitat"} palette covers every level of the habitat vocabulary in
three tiers. The five zones sampled on most trips take the filled shapes
(21 to 25), which draw with a \code{fill} and an outline \code{colour}. Zones sampled
occasionally take the matching open shapes (0, 1, 2, 5, 6), and zones rarely
sampled take line shapes (3, 4, 8). On a map the convention is that exposure
fills the marker and habitat sets its shape.
}
\examples{
ps_shapes()             # list available shape palettes
ps_shapes("habitat")    # named integer vector

}
\seealso{
\code{\link[=scale_shape_ps]{scale_shape_ps()}}, \code{\link[=ps_colors]{ps_colors()}}, \code{\link[=ps_habitat_colors]{ps_habitat_colors()}}
}
