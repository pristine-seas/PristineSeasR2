% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_taxonomic_ranks.R
\name{get_taxonomic_ranks}
\alias{get_taxonomic_ranks}
\title{Get Higher Taxonomic Ranks from WoRMS by AphiaID}
\usage{
get_taxonomic_ranks(
  aphia_ids,
  ranks = c("kingdom", "phylum", "class", "order", "family", "genus")
)
}
\arguments{
\item{aphia_ids}{Numeric (or coercible) vector of AphiaIDs to look up.
\code{NA} values are returned as all-\code{NA} rows without an API call.}

\item{ranks}{Character vector of taxonomic ranks to return as columns, in
the order they should appear in the output. Matched case-insensitively
against WoRMS' rank labels. Defaults to
\code{c("kingdom", "phylum", "class", "order", "family", "genus")}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per element of
\code{aphia_ids} and columns \code{accepted_aphia_id} followed by one column per
entry in \code{ranks}.
}
\description{
Looks up the full taxonomic classification for one or more AphiaIDs from
the World Register of Marine Species (\href{https://www.marinespecies.org/}{WoRMS})
via \code{\link[worrms:wm_classification]{worrms::wm_classification()}}, and reshapes it into one row per AphiaID
with one column per requested rank (\code{kingdom}, \code{phylum}, \code{class}, \code{order},
\code{family}, \code{genus} by default).

This is the lookup typically needed after matching field-recorded taxon
names against WoRMS (e.g. with \code{\link[worrms:wm_records_names]{worrms::wm_records_names()}}) to fill in
the higher-rank classification for taxa that are new to a reference table.
}
\details{
A failed lookup for one AphiaID — an invalid or retired ID, a name WoRMS
doesn't recognize, a network hiccup — never stops the whole batch: that
ID's row comes back with \code{NA} in every rank column rather than an error.
Failures are silent by design (no message per failed ID); check for
all-\code{NA} rows in the output if you need to audit them.

Every rank named in \code{ranks} is guaranteed to appear as a column in the
output, even if \emph{none} of the queried taxa have a value at that rank. A
naive pivot-to-wide reshape only creates a column for ranks that actually
show up in the data, which would silently drop that rank from the output
instead of reporting it as missing — this function fills it with \code{NA}
instead, so a downstream join on a fixed set of expected columns doesn't
fail just because this batch happened not to touch that rank.
\subsection{Parallelizing lookups}{

Lookups run one AphiaID at a time, through \code{\link[furrr:future_map]{furrr::future_map_dfr()}} when
the \pkg{furrr} package is installed (falling back to \code{\link[purrr:map_dfr]{purrr::map_dfr()}}
otherwise, which is always sequential). To parallelize, set a plan
\emph{before} calling this function — the function itself never changes the
plan:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{future::plan(future::multisession, workers = 4)
get_taxonomic_ranks(new_taxa$accepted_aphia_id)
}\if{html}{\out{</div>}}

Without an explicit plan (or without \pkg{furrr} installed), lookups run
sequentially.
}
}
\examples{
\dontrun{
# sequential
get_taxonomic_ranks(c(125286, 254941))

# only kingdom/phylum/class
get_taxonomic_ranks(c(125286, 254941), ranks = c("kingdom", "phylum", "class"))

# parallel, e.g. for a large batch of newly-encountered taxa
future::plan(future::multisession, workers = 4)
get_taxonomic_ranks(new_taxa$accepted_aphia_id)
}

}
\seealso{
\code{\link[=clean_field_names]{clean_field_names()}} to standardize taxon names before matching
them against WoRMS; \code{\link[worrms:wm_records_names]{worrms::wm_records_names()}} for the name-matching
step that typically precedes this lookup.
}
