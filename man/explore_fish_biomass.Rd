% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_fish_biomass.R
\name{explore_fish_biomass}
\alias{explore_fish_biomass}
\title{Build the Interactive Fish Survey Results Map (Metric Toggle by Site)}
\usage{
explore_fish_biomass(
  stations,
  sites,
  title = NULL,
  subtitle = "National Geographic Pristine Seas",
  export_path = NULL
)
}
\arguments{
\item{stations}{A station-level data frame (one row per station), such as
\code{station_summary}. Must include \code{ps_site_id}, \code{divers}, \code{depth_strata},
\code{depth_m}, \code{n_taxa}, \code{avg_density_m2}, and \code{avg_biomass_gm2}.}

\item{sites}{A site-level data frame with \code{ps_site_id}, \code{longitude},
\code{latitude}, \code{region}, \code{subregion}, \code{locality}, \code{habitat}, and \code{exposure}
— such as the validated UVS sites table — used to attach coordinates and
metadata. Every site referenced in \code{stations} must have a match here.}

\item{title}{Map title shown in the banner on the \emph{exported} standalone
map only (see \code{export_path}) — not shown on the version this function
returns, since an inline report already has its own section heading.
Required if \code{export_path} is supplied.}

\item{subtitle}{Small text under the title in the exported map's banner.
Set to \code{""} to hide it. Default \code{"National Geographic Pristine Seas"}.}

\item{export_path}{If supplied, a self-contained standalone HTML copy of
the map (with the title banner) is saved to this path via
\code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}. If \code{NULL} (the default), nothing is saved.}
}
\value{
A \code{leaflet} htmlwidget (without the title banner) — print it
directly to display it, e.g. as the last expression in a report chunk.
}
\description{
Builds the standard Pristine Seas per-site fish survey map: station-level
taxa richness, density, and biomass are averaged up to one circle marker
per site, with both marker size and color driven by whichever metric is
active. A layer control (top right) toggles between the three metrics,
swapping the color ramp and the gradient legend (bottom right) to match.
This is the same map used across UVS/BLT fish reports, so calling it
reproduces the exact same metrics, colors, and layout expedition to
expedition.
}
\examples{
\dontrun{
m <- explore_fish_biomass(
  stations    = station_summary,
  sites       = uvs_sites,
  title       = "Vanuatu 2025 Expedition — Fish survey results",
  export_path = file.path(data_out, "blt_results_map.html")
)
m
}

}
