% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_and_export_csv.R
\name{validate_and_export_csv}
\alias{validate_and_export_csv}
\title{Validate a Data Frame's Columns Against a BigQuery Table, Then Export to CSV}
\usage{
validate_and_export_csv(
  df,
  table_ref,
  path,
  label = table_ref,
  project = "pristine-seas",
  quiet = FALSE
)
}
\arguments{
\item{df}{A data frame to validate and export.}

\item{table_ref}{BigQuery table reference in \code{"dataset.table"} form, e.g.
\code{"uvs.lpi_stations"}.}

\item{path}{File path to write the CSV to.}

\item{label}{Short label used in messages and the error, e.g. \code{"Stations"}.
Defaults to \code{table_ref}.}

\item{project}{BigQuery project ID. Defaults to \code{"pristine-seas"}.}

\item{quiet}{If \code{TRUE}, suppresses the confirmation message on success.
Default \code{FALSE}.}
}
\value{
The \code{path} the CSV was written to, invisibly.
}
\description{
Before writing a pipeline's output to CSV for upload, this checks the data
frame's columns against the \emph{live} schema of the BigQuery table it's headed
to — catching a renamed column upstream, a new field the database doesn't
know about yet, or a dropped column, at export time rather than at upload
time (or, worse, silently after upload).

If the columns match exactly (same names, any order), the data frame is
reordered to match the table's column order and written to \code{path}. If they
don't, nothing is written and the function errors with the specific
missing/extra column names.
}
\examples{
\dontrun{
stations_csv <- validate_and_export_csv(
  df        = lpi_stations,
  table_ref = "uvs.lpi_stations",
  path      = file.path(data_out, paste0(exp_id, "_uvs_lpi_stations.csv")),
  label     = "Stations"
)
}

}
