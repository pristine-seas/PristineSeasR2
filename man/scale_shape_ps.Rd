% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapes.R
\name{scale_shape_ps}
\alias{scale_shape_ps}
\title{Discrete shape scale using Pristine Seas shape palettes}
\usage{
scale_shape_ps(palette, drop = FALSE, ...)
}
\arguments{
\item{palette}{Character. Shape palette name passed to \code{\link[=ps_shapes]{ps_shapes()}}.}

\item{drop}{Logical. Passed to \code{\link[ggplot2:scale_manual]{ggplot2::scale_shape_manual()}}. Default
\code{FALSE} to preserve palette order even if levels are unused.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:scale_manual]{ggplot2::scale_shape_manual()}}.}
}
\value{
A ggplot2 shape scale.
}
\description{
Convenience wrapper around \code{\link[ggplot2:scale_manual]{ggplot2::scale_shape_manual()}} that pulls
shapes from \code{\link[=ps_shapes]{ps_shapes()}}, the way \code{\link[=scale_fill_ps]{scale_fill_ps()}} pulls colours from
\code{\link[=ps_colors]{ps_colors()}}.
}
\details{
Filled shapes (21 to 25) take their interior from the \code{fill} aesthetic and
their outline from \code{colour}, so a site map that sets \code{fill = exposure} and
\code{shape = habitat} needs both \code{scale_fill_ps("exposure")} and this scale.
Open and line shapes draw entirely in \code{colour} and ignore \code{fill}.
}
\examples{
library(ggplot2)

# Sites on a map: exposure fills the marker, habitat sets its shape
sites <- data.frame(
  lon      = c(-171.2, -171.1, -171.3, -171.0, -171.15),
  lat      = c(-13.9, -13.8, -13.85, -13.95, -13.75),
  habitat  = c("fore_reef", "back_reef", "patch_reef", "wall", "channel_pass"),
  exposure = c("windward", "lagoon", "sheltered", "exposed", "channel")
)

ggplot(sites, aes(lon, lat, shape = habitat, fill = exposure)) +
  geom_point(size = 4, colour = ps_ink("map")[["title"]], stroke = 0.6) +
  scale_shape_ps("habitat", drop = TRUE) +
  scale_fill_ps("exposure", drop = TRUE) +
  guides(fill = guide_legend(override.aes = list(shape = 21))) +
  theme_ps_map()

}
