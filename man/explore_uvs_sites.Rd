% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_uvs_sites.R
\name{explore_uvs_sites}
\alias{explore_uvs_sites}
\title{Build the Interactive UVS Sites Map (Color-By Region/Subregion/Habitat/Exposure)}
\usage{
explore_uvs_sites(
  sites,
  region_palette,
  subregion_palette,
  habitat_palette = ps_colors("uvs_habitats"),
  exposure_palette = ps_colors("exposure"),
  title = NULL,
  subtitle = "National Geographic Pristine Seas",
  export_path = NULL
)
}
\arguments{
\item{sites}{A site-level data frame (one row per site), such as the
validated UVS sites table. Must include \code{ps_site_id}, \code{longitude},
\code{latitude}, \code{region}, \code{subregion}, \code{habitat}, and \code{exposure}. Optional
columns used in the popup if present: \code{site_name}, \code{locality}, \code{date},
\code{time}, \code{in_mpa}, \code{mpa_notes}, \code{notes}.}

\item{region_palette, subregion_palette}{Named character vectors mapping
each region/subregion name to a hex color. Expedition-specific (region
and subregion names differ every expedition), so there's no default —
define these once in the expedition's setup script.}

\item{habitat_palette, exposure_palette}{Named character vectors mapping
each habitat/exposure category to a hex color. Default to
\code{\link[=ps_colors]{ps_colors()}}'s \code{"uvs_habitats"} and \code{"exposure"} palettes, which are
fixed taxonomies shared across every expedition. A level with no color
in the supplied palette gets a generated fallback color (with a
warning) rather than failing.}

\item{title}{Map title shown in the banner on the \emph{exported} standalone
map only (see \code{export_path}) — not shown on the version this function
returns, since an inline report already has its own section heading.
Required if \code{export_path} is supplied.}

\item{subtitle}{Small text under the title in the exported map's banner.
Set to \code{""} to hide it. Default \code{"National Geographic Pristine Seas"}.}

\item{export_path}{If supplied, a self-contained standalone HTML copy of
the map (with the title banner) is saved to this path via
\code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}. If \code{NULL} (the default), nothing is saved.}
}
\value{
A \code{leaflet} htmlwidget (without the title banner) — print it
directly to display it, e.g. as the last expression in a report chunk.
}
\description{
Builds the standard Pristine Seas UVS site map: one marker per site, with
a "Color by" toggle (top right) to recolor markers by region, subregion,
habitat, or exposure, a rich popup (location, habitat, exposure, survey
date, MPA status, notes), and a search box (top left) to jump to a site
by ID or name. This is the same map used across UVS site reports, so
calling it with the standard habitat/exposure palettes reproduces the
exact same look expedition to expedition.
}
\examples{
\dontrun{
m <- explore_uvs_sites(
  sites             = uvs_sites,
  region_palette    = region_palette,
  subregion_palette = subregion_palette,
  title             = "Vanuatu 2025 Expedition — Underwater Visual Survey sites",
  export_path       = file.path(data_out, "dive_site_map.html")
)
m
}

}
