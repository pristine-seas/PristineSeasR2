% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_parts.R
\name{ps_ink}
\alias{ps_ink}
\alias{ps_theme_colors}
\title{The Ink Behind The Themes}
\usage{
ps_ink(theme = c("chart", "map"))

ps_theme_colors(theme = c("chart", "map"))
}
\arguments{
\item{theme}{Either \code{"chart"} (default) or \code{"map"}.}
}
\value{
A named character vector of hex codes. Names common to both: \code{canvas},
\code{panel}, \code{grid}, \code{strip}, \code{title}, \code{body}, \code{muted}.
}
\description{
The ink behind \code{\link[=theme_ps]{theme_ps()}} and \code{\link[=theme_ps_map]{theme_ps_map()}}. Use it when a figure needs
to draw something the theme does not supply — an EEZ hairline, a text halo, a
reference line, land polygons under a raster — and that mark has to sit in
the same key as the canvas around it.

\code{"chart"} returns the paper palette; \code{"map"} returns the water palette plus
the geography colors (\code{land}, \code{coast}, \code{eez}, \code{shelf}) that a hand-built
basemap needs.

\code{ps_theme_colors()} is the original name for the same function. It is kept as
a plain alias — no warning, no behaviour of its own — because it is called
across the expedition pipelines. New code should prefer \code{ps_ink()}, which does
not read as a transposition of \code{\link[=theme_ps]{theme_ps()}} and is not mistaken for
\code{\link[=ps_colors]{ps_colors()}}, the categorical palettes for data rather than for canvas.
}
\examples{
ps_ink("chart")
ps_ink("map")[["eez"]]

\dontrun{
ink <- ps_ink("map")

ggplot() +
  geom_spatvector(data = eez, fill = NA, colour = ink[["eez"]], linetype = "22") +
  geom_spatvector(data = land, fill = ink[["land"]], colour = ink[["coast"]]) +
  theme_ps_map()
}
}
\seealso{
\code{\link[=ps_colors]{ps_colors()}} for the \emph{data} palettes — trophic groups, habitats,
functional groups — which encode categories rather than chrome.
}
