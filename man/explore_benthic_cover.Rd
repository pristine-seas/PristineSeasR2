% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_benthic_cover.R
\name{explore_benthic_cover}
\alias{explore_benthic_cover}
\title{Build the Interactive Benthic Cover Map (Pie Charts by Site)}
\usage{
explore_benthic_cover(
  stations,
  sites,
  title = NULL,
  subtitle = "National Geographic Pristine Seas",
  export_path = NULL,
  cover_groups = default_benthic_cover_groups()
)
}
\arguments{
\item{stations}{A station-level data frame (one row per station), such as
\code{lpi_stations}. Must include \code{ps_site_id}, \code{region}, \code{subregion},
\code{locality}, \code{habitat}, \code{exposure}, \code{divers}, and the percent-cover
columns referenced by \code{cover_groups$cols}. A cover column that's missing
entirely is treated as 0 for that group, so a protocol that doesn't
track every group still works.}

\item{sites}{A site-level data frame with \code{ps_site_id}, \code{longitude}, and
\code{latitude} — such as the validated UVS sites table — used to attach
coordinates. Every site referenced in \code{stations} must have a match here.}

\item{title}{Map title shown in the banner on the \emph{exported} standalone
map only (see \code{export_path}) — not shown on the version this function
returns, since an inline report already has its own section heading.
Required if \code{export_path} is supplied.}

\item{subtitle}{Small text under the title in the exported map's banner.
Set to \code{""} to hide it. Default \code{"National Geographic Pristine Seas"}.}

\item{export_path}{If supplied, a self-contained standalone HTML copy of
the map (with the title banner) is saved to this path via
\code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}. If \code{NULL} (the default), nothing is saved.}

\item{cover_groups}{A data frame with columns \code{group} (display name),
\code{cols} (one or more \code{stations} column names, comma-separated, summed
into this group), and \code{color} (hex color for the pie slice/legend).
Defaults to \code{\link[=default_benthic_cover_groups]{default_benthic_cover_groups()}}; override only if a
protocol's cover columns genuinely differ from the LPI standard.}
}
\value{
A \code{leaflet} htmlwidget (without the title banner) — print it
directly to display it, e.g. as the last expression in a report chunk.
}
\description{
Builds the standard Pristine Seas per-site benthic cover map: station-level
percent cover is averaged up to one site, then shown as a pie chart on a
satellite basemap, with a rich popup (location, habitat, exposure, station
count, dive team) and a legend of functional-group colors. This is the
same map used across UVS/LPI benthic reports, so calling it with the
default \code{cover_groups} reproduces the exact same groups, colors, and
layout expedition to expedition.
}
\examples{
\dontrun{
m <- explore_benthic_cover(
  stations    = lpi_stations,
  sites       = uvs_sites,
  title       = "Vanuatu 2025 Expedition — Benthic composition",
  export_path = file.path(data_out, "lpi_results_map.html")
)
m
}

}
