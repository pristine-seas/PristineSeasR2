% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_s2_detections.R
\name{ee_connect}
\alias{ee_connect}
\title{Connect to Google Earth Engine}
\usage{
ee_connect(
  project,
  python = Sys.getenv("EARTHENGINE_PYTHON"),
  asset_home = NULL,
  force = FALSE
)
}
\arguments{
\item{project}{Earth Engine cloud project to bill and authenticate against,
e.g. \code{"pristine-seas"}.}

\item{python}{Path to a Python interpreter that has \code{earthengine-api}
installed. Defaults to the \code{EARTHENGINE_PYTHON} environment variable; pass
\code{""} to leave \code{reticulate}'s own choice alone.}

\item{asset_home}{The account's Earth Engine asset root, e.g.
\code{"users/jsmith"}. Optional, and only needed if \code{rgee} stalls on the asset
root prompt described above.}

\item{force}{Re-open the connection even if one was already made this
session. Default \code{FALSE}.}
}
\value{
\code{TRUE}, invisibly.
}
\description{
Opens the \code{rgee} connection that \code{\link[=explore_s2_detections]{explore_s2_detections()}} renders its crops
through, working around two things that otherwise make \code{rgee} unusable from a
knitr chunk:
\itemize{
\item \code{rgee} asks Earth Engine for the account's legacy asset root, but the
\code{earthengine-api} no longer answers that call — it returns nothing whether
or not a home exists. \code{rgee} then prompts for a folder name, and
\code{readline()} returns \code{""} instantly in a non-interactive chunk, so the
prompt loops on an answer it will never get. Supplying \code{asset_home} skips
the lookup entirely.
\item \code{reticulate} does not read \code{EARTHENGINE_PYTHON} — that variable is \code{rgee}'s
— and defaults to its own managed environment, which has no
\code{earthengine-api}. \code{rgee} then reports the interpreter it \emph{wanted} rather
than the one in use, so the error names a Python that does have the package.
Binding the interpreter before \code{rgee} touches Python at all avoids it.
}

Calling this more than once in a session is free: the connection is opened on
the first call and remembered.
}
\examples{
\dontrun{
ee_connect(project = "pristine-seas", asset_home = "users/jsmith")
}

}
