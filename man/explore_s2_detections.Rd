% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_s2_detections.R
\name{explore_s2_detections}
\alias{explore_s2_detections}
\title{Explore Sentinel-2 Vessel Detections, With The Imagery Behind Each One}
\usage{
explore_s2_detections(
  detections,
  cache_dir = NULL,
  buffer_m = 500,
  view = "ocean",
  embed = TRUE,
  boundary = NULL,
  boundary_color = "#B6D94C",
  matched_color = "#4EC9E8",
  unmatched_color = "#8A949E",
  class_col = NULL,
  palette = NULL,
  gallery = TRUE,
  gallery_order = "by detection score",
  legend_title = NULL,
  title = NULL,
  subtitle = "National Geographic Pristine Seas",
  export_path = NULL,
  ee_project = NULL,
  ee_python = Sys.getenv("EARTHENGINE_PYTHON"),
  ee_asset_home = NULL
)
}
\arguments{
\item{detections}{A detection table, or a path to a CSV of one — such as a
pull from GFW's \code{detect_scene_match_pipe} tables. Must include \code{detect_id},
\code{detect_lat}, \code{detect_lon}, \code{scene_id}, \code{presence_score} and \code{cloud_score}.
Optional columns used when present: \code{detect_timestamp} (or \code{when}),
\code{length_m_inferred} (or \code{length_m}),
\code{matched} (logical), and \code{mmsi} — or \code{ssvid}, GFW's name for the same
number on AIS data. Each adds a line to the popup and the crop's caption;
\code{matched}, or \code{ssvid} standing in for it, is also what splits the markers
into matched and unmatched, and the MMSI is what makes a matched detection
a vessel you can look up rather than just an object on the water.}

\item{cache_dir}{Directory the crops are cached in, one PNG per detection.
Defaults to a session temporary directory, which means every crop is pulled
again next session — pass a real path for anything you intend to re-render.}

\item{buffer_m}{Half-width of the crop, in metres: the frame is twice this
across. Default \code{500}, so a 1 km view in which a 30 m hull is three pixels.}

\item{view}{Which stretch to render: \code{"ocean"} (the default, and the one that
separates a hull from a wave crest), \code{"natural"}, \code{"nir"}, or
\code{"false_colour"}. Give it more than one and each detection is shown in all
of them, side by side — \code{c("ocean", "nir")} is the useful pair, since water
absorbs near-infrared almost entirely and anything solid comes back bright.
Every extra view is another crop to render, cache and inline, so the cost
and the file size scale with the number asked for.}

\item{embed}{Inline each crop into the page as a \verb{data:} URI. \code{TRUE}, the
default, and the only setting that produces a map worth sending anyone.
\code{FALSE} links to the Earth Engine URLs instead — lighter, but the map stops
working when those expire, which takes days.}

\item{boundary}{Optional \code{sf} polygon drawn over the imagery, e.g. an MPA.}

\item{boundary_color}{Colour of that outline. Default \code{"#B6D94C"}.}

\item{matched_color, unmatched_color}{Marker colours for detections matched to
AIS and detections not matched. Ignored when \code{class_col} is active.}

\item{class_col}{Name of a column giving each matched detection an identity —
a vessel type, say — beyond the plain matched/unmatched split. Defaults to
\code{"type"} when present, so a table built with a vessel-type join needs no
argument at all; \code{NULL} keeps the plain two-colour behaviour regardless of
what the table carries. The same generalisation \code{\link[=explore_s1_detections]{explore_s1_detections()}}
already makes, for the same reason: a match is an identity, not just a
broadcast.}

\item{palette}{Named colours, one per level of \code{class_col}. Defaults to a
grey ramp when \code{class_col} is active and no palette is given.}

\item{gallery}{Give the exported map a second pane: every detection as a
thumbnail, ordered by detection score, linked both ways to the map. Clicking
a thumbnail flies the map there and opens the crop; opening a marker's popup
scrolls the gallery to its thumbnail. \code{TRUE} by default, and it adds nothing
to the file — the thumbnails are crops the page already carries. The widget
this function \emph{returns} is always the plain map, since a two-pane dashboard
wants a full window rather than a report's figure column.}

\item{gallery_order}{How the contact sheet is sorted, named under its count.
Rows are drawn in the order they arrive, so this labels that order rather
than imposing one.}

\item{legend_title}{Heading over the legend. Defaults to the detection count.}

\item{title}{Map title shown in the banner on the \emph{exported} map only (see
\code{export_path}) — not on the widget this returns, since an inline report
already has its own heading. Required if \code{export_path} is supplied.}

\item{subtitle}{Small text under that title. \code{""} hides it.}

\item{export_path}{If supplied, a self-contained standalone HTML copy is
saved here. This is the shareable artefact, and with \code{embed = TRUE} it is
the whole tool in one file.}

\item{ee_project, ee_python, ee_asset_home}{Passed to \code{\link[=ee_connect]{ee_connect()}} if — and
only if — a crop has to be rendered. \code{ee_project} is required in that case.}
}
\value{
A \code{leaflet} htmlwidget. Print it to display it inline; or ignore the
return value and use \code{export_path}, which is usually the better read since
the map wants a full window.
}
\description{
Builds the standard Pristine Seas Sentinel-2 detection map: one marker per
detection on satellite imagery, a popup carrying the model's scores and the
scene the detection came from, and — behind a \strong{View Sentinel-2 thumbnail}
link — the crop itself, opened full-size with a red ring marking the exact
detection position. Arrow keys step through every detection in turn, so a
few hundred candidates can be reviewed without going back to the map.

Crops are rendered by Earth Engine, cached to \code{cache_dir}, and inlined into
the page, which is what lets the exported map travel: send the HTML to a
colleague and it opens with no Earth Engine account, no credentials, and no
access to the data behind it. Only the basemap is fetched live.

Earth Engine is contacted \strong{only for crops not already cached}, so a map
whose cache is warm needs no credentials at all and builds in about a second.
}
\examples{
\dontrun{
explore_s2_detections(
  detections  = s2_detections,
  cache_dir   = file.path(gfw_dir, "s2_thumbs"),
  boundary    = mpa,
  title       = "Sentinel-2 detections in Bikar-Bokak since designation",
  export_path = file.path(fig_dir, "s2_detections.html"),
  ee_project  = "pristine-seas"
)
}

}
