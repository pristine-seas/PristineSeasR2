% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{rmi_2023_uvs_sites}
\alias{rmi_2023_uvs_sites}
\title{Underwater visual survey sites of the 2023 Marshall Islands expedition}
\format{
A tibble with 60 rows and 13 columns:
\describe{
\item{ps_site_id}{Site identifier, \code{RMI_2023_uvs_001} to \verb{_060}.}
\item{exp_id}{Expedition identifier, \code{RMI_2023} throughout.}
\item{region, subregion}{The atoll: Bikar, Bokak, Bikini or Rongerik.
Each atoll was its own region on this expedition, so the two agree.}
\item{locality}{A named place within the atoll, where one was recorded.}
\item{date, time}{When the survey began, local. \code{time} is an \code{hms}.}
\item{latitude, longitude}{Position in decimal degrees, WGS84.}
\item{site_name}{The team's short name for the site, such as \code{BKR-6}.}
\item{habitat}{\code{fore_reef}, \code{back_reef} or \code{patch_reef}, from
\link{allowed_vocab}\verb{$uvs_habitats}.}
\item{exposure}{\code{windward}, \code{leeward} or \code{lagoon}, from
\link{allowed_vocab}\verb{$exposure}.}
\item{in_mpa}{Whether the site lies inside a marine protected area.}
}
}
\source{
Pristine Seas expedition database, \code{pristine-seas.uvs.sites},
expedition \code{RMI_2023}.
}
\usage{
rmi_2023_uvs_sites
}
\description{
The sites surveyed by the Pristine Seas expedition to the northern atolls
of the Marshall Islands in 2023, one row per site, in the standard shape
the package's UVS functions expect. Bundled so examples, articles and tests
have a real expedition to draw on without a database or Drive connection.

A dated snapshot of the canonical table in BigQuery
(\code{pristine-seas.uvs.sites}), taken by the script in \verb{data-raw/}. The team
lead and the free-text field notes are left in the database; every column
the figures use is here.
}
\examples{
rmi_2023_uvs_sites

table(rmi_2023_uvs_sites$region, rmi_2023_uvs_sites$habitat)
}
\seealso{
\code{\link[=map_uvs_sites]{map_uvs_sites()}} and \code{\link[=explore_uvs_sites]{explore_uvs_sites()}}, which take this table
as their \code{sites}.
}
\keyword{datasets}
