% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_uvs_sites.R
\name{map_uvs_sites}
\alias{map_uvs_sites}
\title{Map underwater visual survey sites}
\usage{
map_uvs_sites(
  sites,
  region = NULL,
  subregion = NULL,
  expand = 0.25,
  min_span = 3,
  extent = NULL,
  basemap = c("esri", "mapbox", "coast", "natgeo"),
  mapbox_token = Sys.getenv("MAPBOX_TOKEN"),
  zoom = NULL,
  label_sites = FALSE,
  locator = TRUE,
  title = "Underwater visual survey sites",
  subtitle = NULL,
  caption = NULL,
  base_size = 12,
  export = NULL,
  width = 8,
  height = NULL
)
}
\arguments{
\item{sites}{A site-level data frame, one row per site, such as the
validated UVS sites table. Must include \code{ps_site_id}, \code{longitude},
\code{latitude}, \code{region}, \code{subregion}, \code{habitat} and \code{exposure}. Habitat and
exposure must follow the vocabulary in \link{allowed_vocab}; a missing exposure
is drawn as \code{"unknown"}.}

\item{region, subregion}{Character. Which sites to draw. Either may name one
or several values; \code{subregion} is matched within \code{region} when both are
given. \code{NULL} (the default) places no restriction, so with neither set the
whole table is drawn. A name that matches no site is an error that lists
the names available.}

\item{expand}{Numeric. Margin around the sites, as a fraction of the larger
side of their bounding box, added on every side. Default 0.25. Raise it
to show more sea around the sites, lower it to tighten the frame.}

\item{min_span}{Numeric. The narrowest the map may be, in kilometres, on
either axis. Default 3.}

\item{extent}{The frame, set outright: \code{c(xmin, ymin, xmax, ymax)} in
degrees of longitude and latitude, or an \code{sf} bbox in any projection.
Overrides \code{expand} and \code{min_span}. \code{NULL} (the default) fits the frame
to the sites. Every map carries the frame it drew as
\code{attr(p, "extent")}, in degrees, so the usual way to nudge one is to
start from that and move an edge.}

\item{basemap}{What the sites are drawn on: \code{"esri"} (World Imagery, the
default), \code{"mapbox"} (Satellite, needs a token), \code{"coast"} (Global
Islands coastline, no imagery) or \code{"natgeo"} (the National Geographic
World Map, for regional views). See the Basemaps section.}

\item{mapbox_token}{Character. A Mapbox access token, used only when
\code{basemap = "mapbox"}. Defaults to the \code{MAPBOX_TOKEN} environment variable.}

\item{zoom}{Integer. Tile zoom level for the tiled basemaps. \code{NULL} (the
default) picks the level that gives the frame's longer side about 2000
pixels, within what each provider reliably serves; one level up doubles
that, and quadruples the fetch. A value given here is honoured up to the
provider's hard limit. Ignored by \code{basemap = "coast"}.}

\item{label_sites}{Logical. Label each marker with its site number, the
digits that end \code{ps_site_id} (so \code{RMI_2023_uvs_007} reads \code{7}), placed by
\strong{ggrepel} so labels never sit on a marker, on each other or on the
globe. An id with no trailing digits is shown whole. Default \code{FALSE}.}

\item{locator}{Logical. Inset a small globe, the hemisphere centred on the
map with a rectangle outlining the frame, so a reader who does not know
the archipelago can place it. It takes the top right corner unless a site
sits there, then the top left, then the bottom right; the compass rose
takes the next free corner. Land comes from Natural Earth through
\strong{rnaturalearth}. Default \code{TRUE}.}

\item{title, subtitle, caption}{Character. The title block. The subtitle
defaults to the place drawn and its site count; the caption to the credit
for the basemap drawn, which each provider's terms ask for.}

\item{base_size}{Numeric. Base font size in points, passed to
\code{\link[=theme_ps_map]{theme_ps_map()}}. Default 12.}

\item{export}{Character. A file path to save the map to as well as
returning it, typically a \code{.pdf} for the report; any extension
\code{ggplot2::ggsave()} knows is accepted. A PDF is vector for everything but
the imagery, which is embedded at its fetched resolution; the house
typeface is embedded where R can draw through Cairo and set in Helvetica
otherwise. \code{NULL} (the default) saves nothing.}

\item{width, height}{Numeric. Size of the exported figure in inches.
\code{width} defaults to 8; \code{height} to \code{NULL}, which sets it from the frame's
own proportions so the map is neither stretched nor padded.}
}
\value{
A ggplot object, with the frame it drew as \code{attr(p, "extent")}.
Print it, or save it with \code{ggsave()}; the theme carries its own canvas,
so no \code{bg} is needed. When \code{export} is given the file is written first
and the map returned invisibly.
}
\description{
Draws the standard Pristine Seas site map for a report: the sites of a
region or subregion on satellite imagery, a reference map or a clean drawn
coastline, each marker taking its shape from the habitat (\code{\link[=ps_shapes]{ps_shapes()}}) and
its fill from the exposure (\code{\link[=ps_colors]{ps_colors()}}), set on \code{\link[=theme_ps_map]{theme_ps_map()}} with a
compass rose, a scale bar and a locator globe kept quiet enough not to
compete with the geography. One call, one design, every expedition.

The frame is fitted to the sites chosen: their bounding box, padded by a
fraction of its larger side, and never narrower than \code{min_span} kilometres
so a tight cluster of sites still shows the reef around it. To adjust it,
widen or tighten \code{expand}, or set the frame outright with \code{extent}. Tiles
are fetched at the zoom that gives the frame's longer side about 2000
pixels, enough for a full-page figure, and cached for the session, so
redrawing a map is quick.
}
\section{Basemaps}{

Four to choose from, and the right one differs map by map:
\itemize{
\item \code{"esri"}, the default: Esri World Imagery. Needs no account. A consistent
mosaic wherever Esri had a clear pass.
\item \code{"mapbox"}: Mapbox Satellite. Often a fresher, sharper Maxar scene where
Esri has cloud, and needs an access token. Rather than pass it to every
call, keep it in \code{.Renviron} as \code{MAPBOX_TOKEN} and it is picked up from
there: \code{usethis::edit_r_environ()}, then a line \code{MAPBOX_TOKEN=pk.xxxx}.
\item \code{"coast"}: no imagery at all. Land from the USGS, Esri and WCMC Global
Islands database, Landsat-derived polygons for every island on Earth,
drawn in the theme's land ink on its water, with the graticule showing.
The quiet option when the imagery is cloudy or the sites are the point.
Polygons are fetched from UNEP-WCMC's map service for the frame drawn, so
nothing needs to be on disk.
\item \code{"natgeo"}: the National Geographic World Map, Esri's reference map in
the Society's cartographic style, with shaded relief, bathymetry and
place names. Light-toned, so the marks flip to dark ink on it. Made for
the regional view: a whole region or expedition, where the names matter
and a single island's reef would be too small to see. Its content is
only whole to zoom 9 over the open ocean, so the automatic zoom stops
there and a frame under about 200 km draws soft. Where a deeper level
turns out to have content, ask for it with \code{zoom}.
}

The caption credits whichever source drew the map, as each provider's terms
ask.
}

\section{What it needs}{

The annotations come from \strong{ggspatial}, and tiled basemaps from
\strong{maptiles} and \strong{tidyterra}, none of which install with this package;
\strong{ggrepel} is added when \code{label_sites = TRUE} and \strong{rnaturalearth} with
\strong{rnaturalearthdata} when \code{locator = TRUE}. The first call offers to
install whichever is missing. Every basemap is fetched live, so an internet
connection is needed.
}

\section{Building on it}{

The return value is an ordinary ggplot, so anything the map lacks can be
added the usual way: a \code{labs()} to change the wording, a \code{geom_sf()} for an
MPA boundary. Take colours for those layers from \code{\link[=ps_ink]{ps_ink()}} so they sit in
the same key as the canvas.
}

\examples{
\dontrun{
map_uvs_sites(rmi_2023_uvs_sites, region = "Bikar")

# Straight to the report
map_uvs_sites(rmi_2023_uvs_sites, region = "Bikar", export = "figures/bikar_sites.pdf")
}

}
\seealso{
\code{\link[=explore_uvs_sites]{explore_uvs_sites()}} for the interactive map, \code{\link[=theme_ps_map]{theme_ps_map()}} for
the canvas, \code{\link[=ps_shapes]{ps_shapes()}} and \code{\link[=ps_colors]{ps_colors()}} for the marker encoding.
}
