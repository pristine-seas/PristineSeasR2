% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_ps.R
\name{gt_theme_ps}
\alias{gt_theme_ps}
\title{Pristine Seas gt Theme for Summary Tables}
\usage{
gt_theme_ps(
  data,
  ...,
  col_labels = NULL,
  spanners = NULL,
  total_col = NULL,
  heatmap_cols = NULL,
  heatmap_fill = c("#FFFFFF", "#407899"),
  grand_summary = FALSE,
  zero_dash = FALSE
)
}
\arguments{
\item{data}{A data frame, already shaped for display.}

\item{...}{Passed to \code{\link[gt:gt]{gt::gt()}} — most often \code{groupname_col} and
\code{rowname_col} for a grouped table.}

\item{col_labels}{Named character vector or list of column relabels, passed
to \code{\link[gt:cols_label]{gt::cols_label()}}. \code{NULL} to skip.}

\item{spanners}{Named list; each element is a character vector of column
names and the element's name is the spanner label shown above them.
\code{NULL} to skip.}

\item{total_col}{Name of a column to set off with bold text and a left
border — e.g. a row-wise \code{Total}. \code{NULL} to skip.}

\item{heatmap_cols}{Character vector of columns to fill on a sequential
scale from 0 to their shared maximum. \code{NULL} to skip.}

\item{heatmap_fill}{Two-color gradient endpoints for \code{heatmap_cols}.}

\item{grand_summary}{Logical; add a bold, shaded grand-summary row summing
\code{heatmap_cols} and \code{total_col} across all groups.}

\item{zero_dash}{Logical; render 0 as an en dash, so a matrix of mostly
zeros reads as structure rather than noise.}
}
\value{
A \code{gt_tbl}. Chain \code{\link[gt:fmt_number]{gt::fmt_number()}}, \code{\link[gt:tab_footnote]{gt::tab_footnote()}}, etc.
afterward to finish.
}
\description{
A general-purpose \code{gt} style for tables that summarize survey effort or
results — region/subregion breakdowns, effort matrices, cover and biomass
compositions. The base look is a bold-headed, row-striped, compact table
with groups rendered as a column.

Every additional treatment is \strong{opt-in via an argument}, so a single flat
table and an effort matrix with a total column and a heatmap fill can share
one function rather than each growing its own hand-tuned \code{gt} pipeline.
}
\details{
The arguments compose in a fixed order: relabel, then spanners, then
alignment, then the heatmap fill, then zero substitution, then the grand
summary, and finally the total column — which is set off last so its bold
text and left border also land on the grand-summary cell when both are used.

\code{heatmap_cols} are colored on a shared scale running from 0 to the maximum
value across all of those columns, so a value is comparable across the whole
block rather than only within its own column.
}
\examples{
\dontrun{
# flat effort table
effort |>
  gt_theme_ps(groupname_col = "region", rowname_col = "subregion")

# cover matrix: relabelled, under a spanner, heat-filled, with a total column
cover_by_subregion |>
  gt_theme_ps(groupname_col = "region",
              rowname_col   = "subregion",
              col_labels    = grps_labels,
              spanners      = list(`\% Cover by Functional Group` = cover_cols),
              heatmap_cols  = cover_cols,
              heatmap_fill  = c("white", "#0b5d4a"),
              zero_dash     = TRUE) |>
  gt::fmt_number(columns = dplyr::any_of(cover_cols), decimals = 1)
}

}
\seealso{
\code{\link[=light_gt]{light_gt()}} for the plain, compact style used for diagnostic tables.
}
